create virtual environment using command: python -m venv venv
activate virtual environment using command: source venv/bin/activate
install required packages using command: pip install -r requirements.txt
run the program using command: python youtube-downloader.py
